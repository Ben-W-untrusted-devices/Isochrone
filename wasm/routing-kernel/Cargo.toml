[package]
name = "routing_kernel"
version = "0.1.0"
edition = "2021"
description = "WASM-ready routing kernel primitives for Isochrone hot paths"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
panic = "abort"
